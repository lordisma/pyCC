coverage:
  range: 60..80
  round: down
  precision: 2
  status:
    project:
      default:
        target: auto
        # this allows a 10% drop from the previous base commit coverage
        threshold: 10%

comment:
  layout: "diff, files"

github_checks:
  annotations: false
